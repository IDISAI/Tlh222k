# kernel-server (Go) — notebook CRUD + auth + CORS. Copy to .env for local dev.

PORT=3006
APP_ENV=development
STORAGE_DIR=./storage/notebooks

# Dev-only auth bypass: treat every request as this role, skip JWT verification.
# MUST be empty in production. Mirrors the web apps' NEXT_PUBLIC_DEV_AUTH_ROLE.
DEV_AUTH_ROLE=super-admin

# Production auth: verify Clerk session JWTs against this JWKS endpoint (leave
# DEV_AUTH_ROLE empty to enable). Find it in the Clerk dashboard (JWKS URL).
CLERK_JWKS_URL=
CLERK_ISSUER=
CLERK_AUDIENCE=

# Browser origins allowed to call this server (CORS).
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3002,http://localhost:3003

# Required for the five-minute browser-to-proxy connection ticket.
SESSION_TICKET_SECRET=replace-with-a-long-random-production-secret

# Container deployments use an internal fixed-policy Docker broker. Leave both
# blank only for host development through dev.mjs.
JUPYTER_BROKER_URL=
JUPYTER_BROKER_TOKEN=
# Linux only: numeric group ID owning /var/run/docker.sock for compose broker.
DOCKER_GID=0

# Sandbox quotas (free-tier defaults).
JUPYTER_MAX_SESSIONS=2
JUPYTER_MAX_SESSIONS_PER_OWNER=1
JUPYTER_SESSION_IDLE_SECONDS=900
JUPYTER_SESSION_CPU=1
JUPYTER_SESSION_MEMORY=2g
JUPYTER_SESSION_PIDS=128
JUPYTER_DOCKER_NETWORK=notebook-internal
