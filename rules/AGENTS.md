# rules - agent notes

Rules here are normative for repo structure.

- Read [packages.md](packages.md) before changing imports across `apps/*` and
  `packages/*`.
- If implementation and rules disagree, investigate before editing. The rules
  may be stale, or the code may have drifted.
- Keep examples using `@workspace/*`.
