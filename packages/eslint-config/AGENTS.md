# packages/eslint-config - agent notes

Shared ESLint config package.

- Exports are `./base`, `./next-js`, and `./react-internal`.
- Changes here affect every workspace package that extends the shared config.
- Keep rule changes intentional and document any repo-wide behavior change.
