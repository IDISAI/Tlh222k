# @workspace/typescript-config

Shared TypeScript configuration for the workspace.

This package contains base configs for apps and libraries. Changes here affect
repo-wide typechecking, so keep framework-specific settings in the appropriate
config file rather than pushing them into every package.
